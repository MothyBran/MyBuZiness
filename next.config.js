/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  reactStrictMode: true,
  // wenn du Images-Loader, Domains etc. nutzt, hier ergänzen
};

module.exports = nextConfig;
